name: telegram

on:
  schedule:
    - cron: "0 13 * * *"   # resumen diario de la jornada (los resultados los envía vigilar.yml
                           # partido a partido, así que el cron de las 04:30 se eliminó)
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: telegram
  cancel-in-progress: false

jobs:
  resumen:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v7
      - run: uv sync --frozen
      - name: Actualizar datos, ajustar ratings y enviar resumen
        run: |
          uv run mundial actualizar
          uv run mundial ratings
          uv run mundial telegram
        env:
          BSD_TOKEN: ${{ secrets.BSD_TOKEN }}
          ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
          FOOTBALL_DATA_KEY: ${{ secrets.FOOTBALL_DATA_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
